import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nomostimelimited.nomosdemo',
  appName: 'AppleLoginTest',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
